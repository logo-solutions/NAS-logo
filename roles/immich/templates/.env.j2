# roles/immich/templates/.env.j2
# Généré par Ansible — ne pas éditer manuellement

DB_HOSTNAME=database
DB_USERNAME=immich
DB_PASSWORD={{ vault_immich_db_password }}
DB_DATABASE_NAME=immich

REDIS_HOSTNAME=redis

UPLOAD_LOCATION={{ immich_data_dir }}
IMMICH_VERSION={{ immich_version }}

JWT_SECRET={{ vault_immich_jwt_secret }}

TZ=Europe/Paris
