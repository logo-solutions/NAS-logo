# alertmanager.yml — généré par Ansible (NAS-logo)

global:
  resolve_timeout: 5m

time_intervals:
  - name: nuit
    time_intervals:
      - times:
          - start_time: "21:00"
            end_time: "24:00"
      - times:
          - start_time: "00:00"
            end_time: "07:00"

route:
  receiver: ntfy
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  mute_time_intervals:
    - nuit

receivers:
  - name: ntfy
    webhook_configs:
      - url: "http://ntfy:80/{{ ntfy_topic }}"
        send_resolved: true
        http_config:
          follow_redirects: true
